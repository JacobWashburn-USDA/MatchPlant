# Core dependencies with exact versions for GPS embedding
piexif==1.1.3
Pillow==10.2.0
pyproj==3.6.1
typing-extensions==4.9.0

# Additional dependencies for image processing and data handling
opencv-python>=4.8.0
numpy>=1.24.0
pandas>=2.0.0

# GUI libraries (for gcp_finder)
matplotlib>=3.7.0

# System-specific GUI dependencies
# For Windows
pywin32>=306; sys_platform == 'win32'
tk>=0.1.0; sys_platform == 'win32'

# For macOS
python-tk>=8.6; sys_platform == 'darwin'
PyObjC>=9.0.1; sys_platform == 'darwin'
PyObjC-core>=9.0.1; sys_platform == 'darwin'

# File operations and utilities
python-dateutil>=2.8.2
shutil; python_version >= '3.0'
