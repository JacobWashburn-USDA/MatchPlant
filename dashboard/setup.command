#!/usr/bin/env bash
# Double-click this file in Finder to run first-time setup.
# No terminal typing required.
cd "$(dirname "$0")"

./setup.sh

echo
read -n 1 -s -r -p "Press any key to close this window..."
echo
